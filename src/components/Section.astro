---
interface Props {
  label: string;
  id?: string;
  href?: string;
}
const { label, id, href } = Astro.props;
---

<section class="section box" id={id}>
  <h2>{href ? <a href={href}>{label}</a> : label}</h2>
  <div><slot /></div>
</section>
